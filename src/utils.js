const API_URL = "http://localhost:5000";

const endpoints = {
    USERS : "users",
    PRODUCTS : "products",
    ORDERS : "orders",
    FAVOURITES : "favourites",
}

const delivery = 50;
const discount = 5;

export {API_URL, endpoints, delivery, discount};